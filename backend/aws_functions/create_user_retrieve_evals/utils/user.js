export async function createNewUser(email, displayName, photoUrl) {
  // Create a new user in the database if one does not exist.
  // If a user does exist, update the user's display name and photo URL.
  console.log(`Creating new user: ${email}, ${displayName}, ${photoUrl}`);
}
